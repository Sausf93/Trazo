# Trazo — mejoras del 17 de agosto de 2026 (sesión multiagente)

Resumen de lo hecho de forma autónoma, todo **verificado por agentes QA** y
**desplegado** (backend Cloud Run rev `00015-97d`). Estado: `pytest` **202** +
calibración **105** verdes, `tsc` limpio, `flutter analyze` sin issues. Repaso de
regresión final: **sólido, cero regresiones** — "listo para la prueba con centro real".

## Para subir a Cloudflare (proyectos existentes, *Create deployment*)
Los **3 zips** de `_deploy/` están frescos con todo lo de abajo:
- `trazo-panel.zip` → proyecto **trazo-panel**
- `trazo-tablet.zip` → **trazo-tablet** (*Not found* = single-page-application)
- `trazo-web.zip` → **trazo-web-af2** (vitrina + `/demo`)

`trazo-admin.zip` sin cambios. El backend ya está desplegado.

## Novedades

### Cierre del bucle clínico
- **Cola de revisión de `sin_valorar`** (panel "Por revisar"): lista los intentos
  que la app no puntúa sola (respuesta hablada, trazo dudoso), muestra qué
  respondió la persona, permite valorar (logrado/parcial/no logrado), **cambiar**
  la decisión o **devolverla a la cola**. Contador en el menú.

### Plan de intervención (diferenciador vs NeuronUP)
- **Objetivos por paciente**: meta por área con barra "objetivo vs situación
  actual", editable, sin duplicados por área. Aparece en el **informe a familia**.
- **Informe a familia por áreas** y **dashboard de centro por áreas** (desempeño
  por dominio, de peor a mejor, para priorizar).

### Datos y cumplimiento
- **Exportar CSV** (historia clínica; individual con nombre real, de centro con
  alias; `;` + BOM para Excel-ES).
- **RGPD**: rectificar el nombre real (art. 16) y **supresión/anonimización** con
  confirmación, solo admin (art. 17).

### Experiencia del mayor (accesibilidad)
- **Audio de instrucciones (TTS)**: lee la consigna en voz alta al empezar cada
  actividad + botón de altavoz para repetir. Para quien no lee bien.
- **Layout espacial real** en "Poner la mesa" y "Arriba o abajo" (cada zona en su
  sitio, no tarjetas de texto sueltas).
- **Más aire entre celdas** en las rejillas y el teclado (para manos que tiemblan).

### Medición más justa
- Secuencia: se puntúa por **orden relativo** (un paso desplazado ya es *parcial*,
  no fracaso).
- Conteo/suma acotados (nada de sumar 30 objetos diminutos); comparaciones con
  margen visible; dinero que **escala por nivel**.
- Contenido: "El intruso" en **positivo** ("…menos una, toca lo que sobra");
  emociones sin el distractor confundible.

### Operación de la maestra
- Marcar **"le ayudé"** desde el monitor en vivo del panel, sin levantarse a la
  tablet.

### Red de seguridad
- **Test de calibración** que genera las ~104 actividades a los 3 niveles y
  verifica que ninguna sea trivial ("se gana sin ejercer la función") ni imposible.
